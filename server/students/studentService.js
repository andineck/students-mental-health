// OPTIONAL

// Right now it's implemented with two layers only: RESTController -> DataAccess

// If ther was more business logic, a third layer (Service) could be helpful
// The access would then be: RESTController -> Service -> DataAccess